> ⚠️ This file is auto-generated by ai-platform-operator.
> Do not edit manually — changes will be overwritten on next reconciliation.

# demo Platform

## Namespace

- Name: `demo`
- Owners: m.redkin@dodopizza.com
- ResourceQuota: CPU 2, Memory 4Gi

## Kubeconfig

Prerequisite: `brew install int128/kubelogin/kubelogin`

```yaml
apiVersion: v1
kind: Config
clusters:
- cluster:
    server: https://89.232.160.44:5443
  name: p-corp-kube
contexts:
- context:
    cluster: p-corp-kube
    user: p-corp-kube
    namespace: demo
  name: demo
current-context: demo
users:
- name: p-corp-kube
```

## Database

- Credentials secret: `demo-db-credentials`
- Get connection details:
```bash
kubectl get secret demo-db-credentials -n demo -o jsonpath='{.data.DB_HOST}' | base64 -d
```

### Using the database in your deployment

Use individual keys: `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`.

## Redis

Per-project in-pod Redis (a single-replica StatefulSet in your namespace).

- Credentials secret: `demo-redis-credentials`
- Service (host): `demo-redis.demo.svc` on port 6379

## Object Storage

- Credentials secret: `demo-bucket-credentials`
- Bucket: `p-aipltf-demo-bucket`
- Endpoint: `https://obs.ru-moscow-1.hc.sbercloud.ru`

## Ingress

- URL: `https://demo.p.dodoteam.ru`
- TLS: automatic (cert-manager, letsencrypt-prod)
- Ingress class: nginx

## Container Registry

- Registry: `ghcr.io/dodo-ai-platform/demo`
- ImagePullSecret: pre-configured in default ServiceAccount (no action needed)

## Monitoring

- Dashboard: https://grafana.p.dodoteam.ru/d/demo

## Observability

- OTLP HTTP endpoint: `alloy.infra-alloy.svc.cluster.local:4318`
- Protocol: OTLP/HTTP (not gRPC — gRPC does not work through ClusterIP on CCE)

## CI/CD

- GitHub Actions workflow is pre-configured in this repository
- Deploy secret: `KUBE_CONFIG_B64` (base64-encoded kubeconfig for deployer SA)

## Security Constraints

- **Pod Security Standards:** restricted — no privileged containers, no hostPath, ports >=1024
- **ResourceQuota:** enforced
